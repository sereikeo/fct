import { Router } from 'express';

export const envelopesRouter = Router();
// TODO: GET    /api/envelopes
// TODO: PUT    /api/envelopes/:id/override  body: { period, overrideAmount }
// TODO: DELETE /api/envelopes/:id/override?period=
