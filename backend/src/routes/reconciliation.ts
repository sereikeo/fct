import { Router } from 'express';

export const reconciliationRouter = Router();
// TODO: GET    /api/reconciliation
// TODO: POST   /api/reconciliation  body: { budgetItemId, date, forecastAmount, actualAmount, note? }
// TODO: PATCH  /api/reconciliation/:id
// TODO: DELETE /api/reconciliation/:id
